services:
  web:
    build: .
    ports:
      - "8001:8080"
    develop:
      watch:
        - action: sync+restart
          path: ./src/WTech.API/bin/Release/net8.0/publish/
          target: /app
      environment:
        - ASPNET_ENVIRONMENT=dev

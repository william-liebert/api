# Modules

This directory contains reusable Terraform modules for AWS EKS resources in the us-east-1 region.

## Available Modules

1. **eks**: Creates EKS cluster and node group resources with ALL values inlined for pet resources

Each module is designed to be reusable and follows Terraform best practices for modularity.